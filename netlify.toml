# Hey AI Hub - Deployed on Netlify
[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"
  SECRETS_SCAN_ENABLED = "false"

# Next.js API routes and admin pages must NOT be redirected to index.html
[[redirects]]
  from = "/api/*"
  to = "/api/:splat"
  status = 200
  force = true

# SPA fallback for client-side routes
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
