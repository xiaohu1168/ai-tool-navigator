# Hey AI Hub — Cloudflare Pages Configuration
# Note: Cloudflare Pages reads env vars from the dashboard, not wrangler.toml
# This file is kept for reference and local development.

[vars]
NEXT_PUBLIC_SITE_URL = "https://heyaihub.com"
NODE_VERSION = "20"
NEXT_RUNTIME = "nodejs"
