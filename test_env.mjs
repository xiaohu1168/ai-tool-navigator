import dotenv from "dotenv"; const result = dotenv.config({ path: ".env", override: false }); let parsed = result.parsed; const expanded = {}; for (const [key, val] of Object.entries(parsed)) { expanded[key] = val.replace(/\$\{([^}]+)\}/g, (_, k) => parsed[k] || "").replace(/\$([A-Za-z_][A-Za-z0-9_]*)/g, (_, k) => parsed[k] || ""); } console.log("Expanded:", expanded.ADMIN_PASSWORD); console.log("Length:", expanded.ADMIN_PASSWORD.length);
